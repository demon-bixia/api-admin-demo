services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

  rabbitmq:
    image: rabbitmq:3-management
    hostname: "rabbitmq"
    ports:
      - 5672:5672
      - 15672:15672
    healthcheck:
      test: [ "CMD", "rabbitmqctl", "status" ]
      interval: 5s
      timeout: 10s
      retries: 5
    env_file:
      - .env

  django:
    build: .
    container_name: django
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 api_admin_demo.wsgi:application
    ports:
      - 8000:8000
    depends_on:
      - postgres
      - rabbitmq
    env_file:
      - .env

  celery-worker:
    build: .
    container_name: celery_worker
    command: celery -A api_admin_demo worker --loglevel=info
    depends_on:
      rabbitmq:
        condition: service_healthy
    env_file:
      - .env

  celery-beat:
    build: .
    container_name: celery_beat
    command: celery -A api_admin_demo beat -l info
    depends_on:
      rabbitmq:
        condition: service_healthy
      celery-worker:
        condition: service_started
    env_file:
      - .env

volumes:
  postgres_data:
